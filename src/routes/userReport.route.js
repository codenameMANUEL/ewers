// import { Router } from "express";
// import userReportController from "../controllers/userReport.controller.js";

// const route = Router();

// route.get("/", userReportController.getAllUsersReport);
// route.get("/:id", userReportController.getUserReport);
// route.post("/", userReportController.createUserReport);
// route.delete("/:id", userReportController.deleteUserReport)


// export default route;
